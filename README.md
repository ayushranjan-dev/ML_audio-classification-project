# Machine Learning Detection Sound (Car Sound Detection)

## Overview
This project trains a CNN on log-mel spectrograms of audio to detect car sounds.

## Setup
1. Create a Python virtual environment:
   ```bash
   python -m venv venv
   source venv/bin/activate   # macOS
   venv\Scripts\activate      